# image-processing
A Python-based image processing toolkit that implements filters, edge detection, thresholding, object segmentation, histogram equalization, and basic computer vision tasks. Built with OpenCV and NumPy and structured as reusable modules with example notebooks.
